metadata:
  vendor: demo.bash
  language: bash

metrics:
  - name: demo.bash.hello
    group_name: demo
    type: string
    description: "Message de test simple depuis un script bash."
    is_critical: false
    command: 'echo "hello from vendor"'

  - name: demo.bash.unix_time
    group_name: demo
    type: numeric
    description: "Timestamp Unix courant."
    is_critical: false
    command: 'date +%s'

  - name: demo.bash.has_systemctl
    group_name: demo
    type: boolean
    description: "Indique si systemctl est disponible sur le système."
    is_critical: false
    command: '[ -x "$(command -v systemctl)" ] && echo true || echo false'
